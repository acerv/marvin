description: test file for unittest
name       : ssh_user_fail 
version    : '1.0'
author     : Andrea Cervesato

protocols:
    ssh:
        address : localtarget
        port    : 22
        user    : sshtest
        password: this_pwd_is_wrong # raises SSHConnectionError
        timeout : 1.0

execute:
    protocol: ssh

    commands:
        - script: "test -d /"
          passing: "0"
          failing: "1"

        - script: "test -d /dhsuojdlsad"
          passing: "0"
          failing: "1"